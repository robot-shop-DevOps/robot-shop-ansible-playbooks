- name: Import the Openresty GPG key
  ansible.builtin.shell:
    cmd: |
      wget -q -O - https://openresty.org/package/pubkey.gpg \
      | gpg --dearmor \
      | tee /usr/share/keyrings/openresty.gpg > /dev/null
    creates: /usr/share/keyrings/openresty.gpg